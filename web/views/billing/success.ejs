<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - SiteScope</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-search me-2"></i>SiteScope
            </a>
            <div class="navbar-nav ms-auto">
                <% if (user) { %>
                    <span class="navbar-text me-3">Hello, <%= user.email %></span>
                    <a class="nav-link" href="/auth/logout">Logout</a>
                <% } %>
            </div>
        </div>
    </nav>

    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6 text-center">
                <div class="card">
                    <div class="card-body">
                        <div class="mb-4">
                            <i class="fas fa-check-circle text-success" style="font-size: 4rem;"></i>
                        </div>
                        <h2 class="text-success mb-3">Payment Successful!</h2>
                        <p class="lead">Thank you for upgrading your plan. Your new features are now active.</p>
                        
                        <% if (sessionDetails) { %>
                        <div class="alert alert-info">
                            <h5>Payment Details</h5>
                            <p><strong>Amount:</strong> $<%= sessionDetails.amount_total / 100 %></p>
                            <p><strong>Payment Method:</strong> <%= sessionDetails.payment_method_types[0] %></p>
                        </div>
                        <% } %>
                        
                        <div class="d-grid gap-2">
                            <a href="/billing/dashboard" class="btn btn-primary">
                                <i class="fas fa-tachometer-alt me-1"></i>View Billing Dashboard
                            </a>
                            <a href="/" class="btn btn-outline-secondary">
                                <i class="fas fa-home me-1"></i>Back to Home
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
