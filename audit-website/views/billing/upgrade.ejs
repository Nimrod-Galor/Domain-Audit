<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - SiteScope</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .tier-card {
            transition: transform 0.2s ease-in-out;
            height: 100%;
        }
        .tier-card:hover {
            transform: translateY(-5px);
        }
        .tier-starter { border-top: 4px solid #28a745; }
        .tier-professional { border-top: 4px solid #007bff; }
        .tier-enterprise { border-top: 4px solid #6f42c1; }
        
        .price {
            font-size: 2.5rem;
            font-weight: bold;
        }
        
        .feature-list {
            list-style: none;
            padding: 0;
        }
        
        .feature-list li {
            padding: 0.5rem 0;
            border-bottom: 1px solid #eee;
        }
        
        .feature-list li:last-child {
            border-bottom: none;
        }
        
        .feature-check {
            color: #28a745;
        }
        
        .current-plan {
            background-color: #f8f9fa;
            opacity: 0.8;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-search me-2"></i>SiteScope
            </a>
            <div class="navbar-nav ms-auto">
                <span class="navbar-text me-3">Hello, <%= user.email %></span>
                <a class="nav-link" href="/auth/logout">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row">
            <div class="col-md-12 text-center mb-5">
                <h1><%= title %></h1>
                <p class="lead">Choose the plan that's right for your website auditing needs</p>
                <p class="text-muted">Current Plan: <strong><%= currentTier.charAt(0).toUpperCase() + currentTier.slice(1) %></strong></p>
            </div>
        </div>

        <div class="row">
            <% tiers.forEach(tier => { %>
            <div class="col-md-4 mb-4">
                <div class="card tier-card tier-<%= tier.name %> <%= tier.name === currentTier ? 'current-plan' : '' %>">
                    <div class="card-header text-center">
                        <h3><%= tier.name.charAt(0).toUpperCase() + tier.name.slice(1) %></h3>
                        <div class="price text-primary">
                            $<%= tier.price %>
                            <small class="text-muted">/month</small>
                        </div>
                    </div>
                    <div class="card-body">
                        <ul class="feature-list">
                            <li>
                                <i class="fas fa-check feature-check me-2"></i>
                                <%= tier.auditsPerMonth === -1 ? 'Unlimited' : tier.auditsPerMonth %> audits per month
                            </li>
                            <li>
                                <i class="fas fa-check feature-check me-2"></i>
                                Up to <%= tier.maxPagesPerAudit === -1 ? 'unlimited' : tier.maxPagesPerAudit %> pages per audit
                            </li>
                            <li>
                                <i class="fas fa-check feature-check me-2"></i>
                                <%= tier.maxExternalLinks === -1 ? 'Unlimited' : tier.maxExternalLinks %> external links checked
                            </li>
                            <li>
                                <i class="fas fa-<%= tier.canAccessFullReports ? 'check feature-check' : 'times text-muted' %> me-2"></i>
                                Full detailed reports
                            </li>
                            <li>
                                <i class="fas fa-<%= tier.canExportPDF ? 'check feature-check' : 'times text-muted' %> me-2"></i>
                                PDF export
                            </li>
                            <li>
                                <i class="fas fa-<%= tier.canScheduleAudits ? 'check feature-check' : 'times text-muted' %> me-2"></i>
                                Scheduled audits
                            </li>
                            <li>
                                <i class="fas fa-<%= tier.hasAPIAccess ? 'check feature-check' : 'times text-muted' %> me-2"></i>
                                API access
                            </li>
                            <li>
                                <i class="fas fa-<%= tier.hasPrioritySupport ? 'check feature-check' : 'times text-muted' %> me-2"></i>
                                Priority support
                            </li>
                        </ul>
                    </div>
                    <div class="card-footer text-center">
                        <% if (tier.name === currentTier) { %>
                            <button class="btn btn-secondary" disabled>
                                <i class="fas fa-check me-1"></i>Current Plan
                            </button>
                        <% } else if (tier.price === 0) { %>
                            <button class="btn btn-outline-primary" disabled>
                                Contact Support for Downgrades
                            </button>
                        <% } else { %>
                            <button class="btn btn-primary" onclick="upgradeToTier('<%= tier.name %>')">
                                <i class="fas fa-arrow-up me-1"></i>Upgrade to <%= tier.name.charAt(0).toUpperCase() + tier.name.slice(1) %>
                            </button>
                        <% } %>
                    </div>
                </div>
            </div>
            <% }); %>
        </div>

        <!-- FAQ Section -->
        <div class="row mt-5">
            <div class="col-md-12">
                <h3 class="text-center mb-4">Frequently Asked Questions</h3>
                <div class="accordion" id="faqAccordion">
                    <div class="accordion-item">
                        <h2 class="accordion-header" id="faqOne">
                            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseOne">
                                Can I change my plan at any time?
                            </button>
                        </h2>
                        <div id="collapseOne" class="accordion-collapse collapse" data-bs-parent="#faqAccordion">
                            <div class="accordion-body">
                                Yes! You can upgrade your plan at any time. For downgrades, please contact our support team to ensure a smooth transition.
                            </div>
                        </div>
                    </div>
                    <div class="accordion-item">
                        <h2 class="accordion-header" id="faqTwo">
                            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseTwo">
                                What happens if I exceed my limits?
                            </button>
                        </h2>
                        <div id="collapseTwo" class="accordion-collapse collapse" data-bs-parent="#faqAccordion">
                            <div class="accordion-body">
                                If you reach your monthly audit limit, you'll need to upgrade your plan or wait until the next billing cycle. Your existing audit data will always remain accessible.
                            </div>
                        </div>
                    </div>
                    <div class="accordion-item">
                        <h2 class="accordion-header" id="faqThree">
                            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseThree">
                                Is there a free trial?
                            </button>
                        </h2>
                        <div id="collapseThree" class="accordion-collapse collapse" data-bs-parent="#faqAccordion">
                            <div class="accordion-body">
                                All new users start with our Freemium plan, which includes 5 audits per month. This gives you a chance to try our service before upgrading.
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Back Button -->
        <div class="row mt-4">
            <div class="col-md-12 text-center">
                <a href="/billing/dashboard" class="btn btn-secondary">
                    <i class="fas fa-arrow-left me-1"></i>Back to Billing Dashboard
                </a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        async function upgradeToTier(tierName) {
            if (!confirm(`Are you sure you want to upgrade to the ${tierName} plan?`)) {
                return;
            }
            
            try {
                const response = await fetch('/billing/create-checkout-session', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ tierName })
                });
                
                const data = await response.json();
                
                if (data.checkoutUrl) {
                    // Redirect to Stripe checkout
                    window.location.href = data.checkoutUrl;
                } else {
                    alert('Error creating checkout session: ' + (data.error || 'Unknown error'));
                }
            } catch (error) {
                console.error('Error:', error);
                alert('Error starting upgrade process. Please try again.');
            }
        }
    </script>
</body>
</html>
