<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Domain Audit</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/styles.css" rel="stylesheet">
</head>
<body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-search me-2"></i>Domain Audit
            </a>
            <% if (user) { %>
                <div class="navbar-nav ms-auto">
                    <a class="nav-link" href="/auth/dashboard">Dashboard</a>
                    <form method="POST" action="/auth/logout" class="d-inline">
                        <button type="submit" class="btn btn-outline-light btn-sm">Logout</button>
                    </form>
                </div>
            <% } %>
        </div>
    </nav>

    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card shadow">
                    <div class="card-body text-center p-5">
                        <% if (success) { %>
                            <div class="mb-4">
                                <i class="fas fa-check-circle fa-4x text-success"></i>
                            </div>
                            
                            <h2 class="card-title text-success mb-4">Email Verified!</h2>
                            
                            <p class="text-muted mb-4">
                                <%= message %>
                            </p>
                            
                            <% if (userInfo) { %>
                                <div class="alert alert-success" role="alert">
                                    <i class="fas fa-user-check me-2"></i>
                                    Welcome <strong><%= userInfo.first_name %></strong>! Your account is now fully activated.
                                </div>
                            <% } %>
                            
                            <div class="d-grid gap-2 mt-4">
                                <% if (user) { %>
                                    <a href="/auth/dashboard" class="btn btn-success">
                                        <i class="fas fa-tachometer-alt me-2"></i>
                                        Go to Dashboard
                                    </a>
                                <% } else { %>
                                    <a href="/auth/login" class="btn btn-success">
                                        <i class="fas fa-sign-in-alt me-2"></i>
                                        Login to Your Account
                                    </a>
                                <% } %>
                                
                                <a href="/" class="btn btn-outline-primary">
                                    <i class="fas fa-home me-2"></i>
                                    Back to Home
                                </a>
                            </div>
                            
                        <% } else { %>
                            <div class="mb-4">
                                <i class="fas fa-exclamation-triangle fa-4x text-danger"></i>
                            </div>
                            
                            <h2 class="card-title text-danger mb-4">Verification Failed</h2>
                            
                            <p class="text-muted mb-4">
                                <%= message %>
                            </p>
                            
                            <div class="alert alert-warning" role="alert">
                                <i class="fas fa-info-circle me-2"></i>
                                <strong>Need help?</strong> Try requesting a new verification email or contact support.
                            </div>
                            
                            <div class="d-grid gap-2 mt-4">
                                <a href="/auth/register" class="btn btn-primary">
                                    <i class="fas fa-user-plus me-2"></i>
                                    Register Again
                                </a>
                                
                                <a href="/auth/login" class="btn btn-outline-primary">
                                    <i class="fas fa-sign-in-alt me-2"></i>
                                    Login
                                </a>
                                
                                <a href="/" class="btn btn-link">
                                    <i class="fas fa-home me-2"></i>
                                    Back to Home
                                </a>
                            </div>
                        <% } %>
                    </div>
                </div>
                
                <div class="text-center mt-4">
                    <small class="text-muted">
                        <i class="fas fa-shield-alt me-1"></i>
                        Your email security is important to us.
                    </small>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
